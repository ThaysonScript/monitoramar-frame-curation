# Núcleo do pipeline de curadoria (amostragem, redundância, clustering,
# manifest e métricas). Sem isso, `monitoramar-curate` não roda.
#
# Instalação:
#   pip install -r requirements.txt
#
# Equivalente a: pip install -e .   (dependências obrigatórias do pyproject.toml)

opencv-python>=4.9
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
PyYAML>=6.0
tqdm>=4.66
