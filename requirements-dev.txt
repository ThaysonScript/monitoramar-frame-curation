# Ferramentas de desenvolvimento (testes e lint), não necessárias para rodar
# o pipeline em produção.
#
# Instalação:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Equivalente a: pip install -e ".[dev]"

pytest>=8.0
ruff>=0.5
